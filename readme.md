# text to api

This is a program that can turn a text file into a working API.

## How it works.

Create a api.txt file and configure it like this.

````
requestType:endpoint:returnMethod:value
````

# notes
You can not put a extra : inside the value.

Run with dotnet run.